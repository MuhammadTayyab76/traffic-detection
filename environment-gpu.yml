name: traffic-detection
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pytorch>=2.2.0
  - torchvision>=0.17.0
  - pytorch-cuda=11.8
  - pip:
      - -r requirements.txt