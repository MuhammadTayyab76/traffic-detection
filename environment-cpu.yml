name: traffic-detection
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pytorch>=2.2.0
  - torchvision>=0.17.0
  - cpuonly
  - pip:
      - -r requirements.txt